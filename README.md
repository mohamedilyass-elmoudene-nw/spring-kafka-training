# spring-kafka-training

To start the development environment, run:

```shell
nix develop
```

This will provide a shell with all the necessary dependencies.

To run the Kafka producer, use the following command:

```shell
task run-producer
```


